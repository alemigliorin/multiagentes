services:
  backend:
    image: ghcr.io/alemigliorin/multiagentes-backend:latest
    restart: unless-stopped
    expose:
      - "8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/tmp:/app/tmp
      - ./backend/videos:/app/videos
    networks:
      - nginx-proxy

  frontend:
    image: ghcr.io/alemigliorin/multiagentes-frontend:latest
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
        - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
        - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}
    restart: unless-stopped
    depends_on:
      - backend
    env_file:
      - ./frontend/.env.production
    expose:
      - "3000"
    networks:
      - nginx-proxy

  frontend-streamlit:
    image: ghcr.io/alemigliorin/multiagentes-streamlit:latest
    restart: unless-stopped
    depends_on:
      - backend
    expose:
      - "8501"
    environment:
      - BACKEND_HOST=http://backend:8000
    volumes:
      - ./backend/tmp:/app/backend/tmp
      - ./backend/videos:/app/backend/videos
    networks:
      - nginx-proxy

networks:
  nginx-proxy:
    external: true
